<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="TabControl.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style BasedOn="{StaticResource StandardTabControl}" TargetType="TabControl" />

    <Style BasedOn="{StaticResource StandardDataGrid}" TargetType="{x:Type DataGrid}">
        <Setter Property="RowDetailsVisibilityMode" Value="VisibleWhenSelected" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="AlternationCount" Value="1" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource AccentColor2}" />
        <Setter Property="SelectionMode" Value="Single" />
    </Style>

</ResourceDictionary>