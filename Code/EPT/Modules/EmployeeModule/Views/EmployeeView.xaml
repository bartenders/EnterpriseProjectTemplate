<UserControl x:Class="EPT.Modules.EmployeeModule.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Behaviors="clr-namespace:EPT.GUI.Behaviors;assembly=EPT.GUI"
             xmlns:CoreControls="clr-namespace:EPT.GUI.Controls;assembly=EPT.GUI"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="43*" />
            <RowDefinition Height="75*" />
            <RowDefinition Height="182*" />
        </Grid.RowDefinitions>
        <CoreControls:SearchTextBox>
            <i:Interaction.Behaviors>
                <Behaviors:InitialFocusBehavior />
            </i:Interaction.Behaviors>
        </CoreControls:SearchTextBox>
        <DataGrid x:Name="Employees" Grid.Row="1" />
        <ContentControl Grid.Row="2"
                        cal:Bind.AtDesignTime="True"
                        cal:View.Model="{Binding CustomerViewModel}" />
    </Grid>
</UserControl>
